# nw-crest-designer

![](https://i.imgur.com/bL1C4DU.gif)

Run with docker:
- Install docker
- Build & image `docker run --rm -it -p 8081:8080 $(docker build -q .)`
- Open your browser and go to localhost:8080
- Kill your container (docker container kill container_name)
